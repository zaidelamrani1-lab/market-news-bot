name: Market News Bot

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: market-news-bot
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run the bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
        run: python bot.py

      - name: Save which articles were processed
        continue-on-error: true
        run: |
          if [ -n "$(git status --porcelain seen.json)" ]; then
            git config user.name "market-news-bot"
            git config user.email "market-news-bot@users.noreply.github.com"
            git add seen.json
            git commit -m "Update seen articles [skip ci]"
            for i in 1 2 3 4 5; do
              git pull --rebase -X ours origin "${GITHUB_REF_NAME}" || git rebase --abort || true
              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                echo "pushed"; break
              fi
              echo "push rejected, retrying..."; sleep 3
            done
          else
            echo "No changes to seen.json"
          fi
